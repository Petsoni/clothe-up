export default defineAppConfig({
    ui: {
        strategy: 'override',
        primary: "stone",
        button: {
            default: {
                variant: 'solid',
            },
        }
    }
})